define(['modules/mainApp'], function (mainApp) {
    mainApp.controller('navigatorController', function ($rootScope, $scope, homeService) {
         
        $scope.singleModel = 1;
                 

        $scope.init = function () {
           
        };

        $scope.init();

    });   

});


